spring:
  application:
    name: habitame-api

  datasource:
    url: jdbc:postgresql://localhost:5432/habitame_db
    username: habitame
    password: Passw0rd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000
